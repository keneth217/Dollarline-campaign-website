# Supabase – copy this file to .env and add your real values (Vite only loads .env)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Create table "volunteers" (Table Editor or SQL):
#   create table volunteers ( id uuid default gen_random_uuid() primary key, name text not null, phone text, email text not null, created_at timestamptz default now() );
# Then run supabase-volunteers-rls.sql in SQL Editor to allow inserts (RLS policy).